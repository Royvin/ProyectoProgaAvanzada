@model IEnumerable<ProyectoPograAvanzada.Models.DetallePedidos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdPedido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdPedido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetalle })
        </td>
    </tr>
}

</table>
